# rc file for launching virtual environments

if [[ -e ~/.bash_profile ]]; then
    source ~/.bash_profile
fi
if [ -f ~/.bash_aliases ]; then
    source ~/.bash_aliases
fi
if [[ -e ~/.bashrc ]]; then
    source ~/.bashrc
fi

source ./env/bin/activate