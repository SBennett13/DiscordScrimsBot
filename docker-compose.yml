---
version: "2.2"
services:
    bot:
        build:
            context: .
            network: host
        container_name: scrims-bot
        restart: always
        network_mode: host
        volumes:
            - "/apps/PugsBot/logs:/apps/PugsBot/logs"
